## Fast-API Тестовый проект

## 🚀 Быстрый старт

## 1. Установка и настройка 

	#Клонируем проект
	
	git clone <repository-url>
	cd Home_Work_4.2.3



# Создаем виртуальное окружение
		python -m venv .venv

# Активируем окружение (выберите вашу ОС)
# Для Windows:
		.venv\Scripts\activate

# Для Linux/Mac:
		source .venv/bin/activate

# Устанавливаем зависимости
		pip install -r requirements.txt

# Настраиваем доступ к API
		cp .env.example .env

## 2. Настройка файла .env:

		#Откройте файл .env в редакторе и заполните:

		BASE_URL=https://api.fast-api.senior-pomidorov.ru
		USER_EMAIL=artem12@mail.ru
		USER_PASSWORD=artem123

## 3. Создание тестовых данных:
		python create_test_data.py -n 20

## 4. Запуск тестов:
		pytest -v

## Что тестируется:

✅ POST /api/v1/items/ — создание элемента

✅ GET /api/v1/items/ — получение списка с пагинацией

✅ PUT /api/v1/items/{id} — обновление элемента

✅ DELETE /api/v1/items/{id} — удаление элемента

❌ Негативные кейсы (ошибки валидации, отсутствие токена и т.д.)

## Команды для быстрого запуска
		#Все в одной команде
		source .venv/bin/activate && python create_test_data.py -n 20 && pytest -v

## Только позитивные тесты:
		pytest tests/test_items_positive.py -v

## Только негативные тесты:
		pytest tests/test_items_negative.py -v

## С Allure отчетом:
		pytest --alluredir=allure-results
		allure serve allure-results

## Структура проекта:
    Home_Work_4.2.3/
    ├── src/                    # Исходный код
    │   ├── api/               # API клиенты
    │   │   └── items_client.py
    │   └── models/            # Pydantic модели
    │       └── schemas.py
    ├── tests/                 # Тесты
    │   ├── conftest.py       # Фикстуры pytest
    │   ├── test_items_positive.py  # Позитивные тесты
    │   └── test_items_negative.py  # Негативные тесты
    ├── create_test_data.py    # Скрипт создания тестовых данных
    ├── requirements.txt       # Зависимости Python
    ├── .env.example          # Пример конфигурации
    └── README.md            # Эта документация
